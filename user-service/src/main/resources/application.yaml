server:
  port: 19100
spring:
  application:
    name: user-service

  datasource:
    url: jdbc:postgresql://localhost:5433/userdb
    username: userdb
    password: userdb
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update  #개발할 때는 update, 후에 validate로 바꾸기
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

jwt:
  secret: ${JWT_SECRET_KEY:bc2a10aaebae00d52a2f7821724ec6b68769177ce2994d11a4f11c0302068bea}
  access-token-expiration: 3600000 # 1 hour in milliseconds
  refresh-token-expiration: 604800000 # 7 days in milliseconds
  access-header-name: Authorization
  header-prefix: "Bearer "

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:19090/eureka/


  # redis
  data:
    redis:
      host: localhost
      port: 6379
      timeout: 3000ms
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 2