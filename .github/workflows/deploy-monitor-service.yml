name: Deploy Monitor Service

on:
  push:
    branches: [ "main", "develop" ]
    paths:
      - 'monitor-service/**'

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Build with Gradle
        working-directory: monitor-service
        run: |
          chmod +x gradlew
          ./gradlew clean build -x test

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.INFRA_DOCKER_USERNAME }}
          password: ${{ secrets.INFRA_DOCKER_PASSWORD }}

      - name: Build and Push Docker Image
        working-directory: monitor-service
        run: |
          docker build -t ${{ secrets.INFRA_DOCKER_USERNAME }}/monitor-service:latest .
          docker push ${{ secrets.INFRA_DOCKER_USERNAME }}/monitor-service:latest

      - name: Deploy to EC2
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.INFRA_EC2_HOST }}
          username: ubuntu
          key: ${{ secrets.INFRA_EC2_SSH_KEY }}
          port: 22
          script: |
            sudo docker stop monitor-service || true
            sudo docker rm monitor-service || true
            
            pkill -f monitor.jar || true

            sudo docker pull ${{ secrets.INFRA_DOCKER_USERNAME }}/monitor-service:latest

            sudo docker run -d --name monitor-service \
              --network host \
              -e "JAVA_TOOL_OPTIONS=-Xms128m -Xmx256m" \
              ${{ secrets.INFRA_DOCKER_USERNAME }}/monitor-service:latest
            
            sudo docker image prune -f